package main

// the firt contact with hands-on in the code.

import (
	"fmt"
)

func main() {
	const name = "Amon"
	const age = 40

	fmt.Printf("This guy named %s is now %d years old. \t and the type is %T and %T", name, age, name, age)
}
